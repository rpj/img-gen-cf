{
  "name": "img-gen",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "remote": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^3.19.0"
  }
}
